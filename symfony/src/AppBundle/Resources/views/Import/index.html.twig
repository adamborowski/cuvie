{% extends 'AppBundle::base.html.twig' %}

{% block body %}
    IMPORT!!
    result: {{ dbg|length}}
    <div>
        <ul>
            {% for entry in dbg %}
                <li>{{ entry }}</li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
